import '../css/index.css';
import {
  mul,
} from './test';

function sum(x, y) {
  return x + y;
}
// eslint-disable-next-line
console.log(sum(1, 3));
// eslint-disable-next-line
console.log(mul(1, 3));